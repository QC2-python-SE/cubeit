Installation
=============

Requirements
------------

- Python >= 3.8
- NumPy >= 1.20.0

Installing CubeIt
-----------------

Install from requirements file:

.. code-block:: bash

   pip install -r requirements.txt

Or install as a package:

.. code-block:: bash

   pip install -e .

